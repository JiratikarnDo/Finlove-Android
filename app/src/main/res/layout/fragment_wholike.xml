<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_constraintWidth_percent="0.48"
    android:padding="16dp"
    android:background="@android:color/white">

    <!-- Title -->
    <TextView
        android:id="@+id/titleLikes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:text="คนที่กดไลค์คุณ"
        android:textColor="#0DD7E9"
        android:textSize="36sp"
        android:textStyle="bold" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/subtitleLikes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="คนเหล่านี้อาจจะชอบคุณก็ได้นะ"
        android:textSize="18sp"
        android:textColor="#000000"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="16dp"/>

    <!-- ครอบด้วย LinearLayout แนวนอน -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="10dp"
        android:orientation="horizontal"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/switchtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="สลับหน้า"
            android:textSize="18sp"
            android:textColor="#000000"/>

        <Switch
            android:id="@+id/switchShowLiked"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textOn="โชว์คนที่คุณกดไลค์"
            android:textOff="โชว์คนที่ไลค์คุณ"
            android:checked="false"
            android:layout_marginEnd="8dp" />

    </LinearLayout>

    <!-- RecyclerView Grid -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewLikes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="2"/>

</LinearLayout>
